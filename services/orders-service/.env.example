# FlashDrop - Orders Service - Environment (LOCAL DEV)
# =====================================================
# Copy to .env and fill in the real values:
#     cp .env.example .env
# WARNING: Never commit the .env file (it is gitignored).
#
# orders-service owns ONLY: client, orders, order_items.
# Other services (Auth/Catalog/Delivery) manage their own databases.
#
# GAP-01 (audit 2026-09-04): this file used to default to
# SPRING_PROFILES_ACTIVE=supabase, which silently ran orders-service against
# the pre-migration Supabase-REST adapters (SupabaseRestClientAdapter /
# SupabaseRestOrderRepositoryAdapter) instead of the migrated architecture
# (JPA + Orders' own PostgreSQL database, per MIGRATION_PLAN.md §1/§6).
# "postgres" is now the explicit, unambiguous default here, matching both
# application.properties' own fallback (spring.profiles.active=${SPRING_PROFILES_ACTIVE:postgres})
# and the real local stack launcher (start-flashdrop-e2e.bat).

SPRING_PROFILES_ACTIVE=postgres

# --- Orders' OWN PostgreSQL database (independent from Auth/Catalog/Delivery) ---
# Local example values only — never put real credentials here.
DB_URL=jdbc:postgresql://localhost:5433/flashdrop_orders
DB_USERNAME=orders_app
DB_PASSWORD=orders_app_pass

# Flyway applies db/migration/V1__init.sql on startup against the URL above.
# Only set FLYWAY_ENABLED=false if you are pointing DB_URL at a database that
# was NOT created fresh by Flyway (e.g. a shared/pre-provisioned instance
# without a baselined flyway_schema_history) — see GAP-06 in the audit report.
# FLYWAY_ENABLED=false

DELIVERY_FEE=2500

AUTH_SERVICE_URL=http://localhost:8081
CATALOG_SERVICE_URL=http://localhost:8082
DELIVERY_SERVICE_URL=http://localhost:8084
INTERNAL_API_KEY=dev-key

RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest

# --- Legacy/transition profile (Supabase REST) — NOT used by default ---
# Only needed if you explicitly set SPRING_PROFILES_ACTIVE=supabase to run
# against the old SupabaseRestClientAdapter/SupabaseRestOrderRepositoryAdapter
# path (application-supabase.properties). Kept for the transition window
# documented in MIGRATION_PLAN.md; do not use for the migrated architecture.
# SUPABASE_URL=https://YOUR-ORDERS-PROJECT.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=PLACEHOLDER_SERVICE_ROLE_KEY

# ============================================================
# Floci / Producción — fuente de cada variable
# ============================================================
# El archivo `.env.example` está pensado para desarrollo local. En Floci
# (Dev → Prod) las variables se inyectan desde las fuentes que se listan
# abajo; la referencia operativa completa es la task definition ECS de
# cada servicio y `infra/floci/INFRASTRUCTURE.md`.
#
# ┌─────────────────────────────────────────────────┬─────────────────────────────────────┐
# │ Variable                                        │ Fuente en Floci                     │
# ├─────────────────────────────────────────────────┼─────────────────────────────────────┤
# │ DB_URL / DB_USERNAME / DB_PASSWORD              │ RDS `orders_db` (ya provisionada)   │
# │                                                 │ más secret `orders/db-password` en  │
# │                                                 │ Floci Secrets Manager (convención   │
# │                                                 │ existente `<servicio>/db-password`,│
# │                                                 │ p.ej. `auth/db-password`,          │
# │                                                 │ `delivery/db-password`). La URL    │
# │                                                 │ exacta y el puerto dinámico los    │
# │                                                 │ obtiene la task definition de       │
# │                                                 │ Orders (escrita por Nicolás).       │
# ├─────────────────────────────────────────────────┼─────────────────────────────────────┤
# │ INTERNAL_API_KEY                                │ **Un solo** secret compartido entre │
# │                                                 │ los 5 despliegues (NO crear un      │
# │                                                 │ secret por servicio: en cuanto se   │
# │                                                 │ rote uno sin el otro, todos los     │
# │                                                 │ `/api/internal/**` empiezan a      │
# │                                                 │ rebotar 403 por desalineación).     │
# │                                                 │ Referenciar el ARN que ya existe.  │
# │                                                 │ Sin esta clave, los endpoints     │
# │                                                 │ internos quedan cerrados en 403    │
# │                                                 │ (el servicio no falla al arranque).│
# ├─────────────────────────────────────────────────┼─────────────────────────────────────┤
# │ AUTH_SERVICE_URL / CATALOG_SERVICE_URL /        │ DNS interno del cluster ECS         │
# │ DELIVERY_SERVICE_URL                            │ (flashdrop-auth:8081, etc.); NO son │
# │                                                 │ URLs públicas.                      │
# ├─────────────────────────────────────────────────┼─────────────────────────────────────┤
# │ RABBITMQ_HOST / PORT / USERNAME / PASSWORD      │ Servicio administrado por el infra-  │
# │                                                 │ structor; en Floci dev es el       │
# │                                                 │ RabbitMQ del compose local.         │
# └─────────────────────────────────────────────────┴─────────────────────────────────────┘
#
# Smoke check post-despliegue (Floci):
#   curl -s http://flashdrop-orders:8083/health
#   # esperado: {"service":"orders-service","status":"ok"}
#   # Orders expone AMBOS /health (HealthController propio, usado por smoke
#   # ad-hoc) y /actuator/health (Spring Actuator, convención del gateway
#   # aggregator para los 4 servicios). Cualquiera de los dos sirve.
#
# Logs a verificar al primer arranque:
#   - "Started OrdersServiceApplication" sin ERROR/WARN de JDBC
#   - "Successfully applied X migrations" (Flyway, sólo si FLYWAY_ENABLED=true)
#   - Sin "Acceso denegado a /api/internal/..." repetidos tras un POST legítimo
#     desde delivery-service (eso indicaría INTERNAL_API_KEY desalineado).
#
# Pre-requisitos operativos (estado actual confirmado por Nicolás 2026-09-10):
#   1. `orders_db` YA existe en Floci (verificado vía `aws rds describe-db-instances`
#      contra `127.0.0.1:4566`). El host port está mapeado. No hay que provisionar.
#   2. Usuario de la BD: `orders_app` (NO `orders_svc` — el
#      `01-postgres-init.sql` se escribió para Coolify y nunca corrió contra
#      Floci, los grants están dados de otra forma). Confirmar contra la BD
#      real antes de declarar el servicio operativo.
#   3. Secret `orders/db-password` YA EXISTE en Floci Secrets Manager con el
#      valor real (NO commitearlo, solo referenciarlo desde la ECS task
#      definition).
