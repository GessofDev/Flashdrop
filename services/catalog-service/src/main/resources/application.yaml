spring:
  application:
    name: catalog-service
  config:
    import: optional:file:.env[.properties]
  profiles:
    default: local

server:
  port: 8082

services:
  internal-api-key: ${INTERNAL_API_KEY:dev-key}
